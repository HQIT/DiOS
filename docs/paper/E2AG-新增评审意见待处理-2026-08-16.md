# E2AG 新增评审意见待处理（2026-08-16）

本表已按 2026-08-17 修订稿逐项复核；验收对象包括 LaTeX、同步 Markdown、算法和最终 PDF。

| 编号 | 评审意见 | 当前判断 | 后续验收条件 | 状态 |
|---|---|---|---|---|
| R1 | 形式化时需要定义什么是“层次” | 已定义授权层集合 $\mathcal H=\{H_E,H_T,H_C\}$ 和对象所属层映射 $\lambda$；状态迁移明确标出 $H_E\rightarrow H_T\rightarrow H_C$ | 问题定义与架构概述均明确授权层不等同于治理/执行/证据平面 | 已验证 |
| R2 | 形式化时需要定义什么是“能力” | 已给出任务作用域能力概念、`Issued` 签发谓词、`ValidGrant` 使用谓词、字段语义和生命周期；明确不主张超出可信治理主机的密码学不可伪造性 | 能力定义、元组、算法签发、PEP 使用和命题证明相互引用 | 已验证 |
| R3 | 算法中的 notation 应在使用前定义 | 已在算法前定义 $\mathcal C$、$\mathcal P$、`targets(e)`、$\tau$、策略返回四元组、辅助过程和局部量；算法不再使用未定义的 $m,U_a,exp$ | 策略显式返回 $\langle d_a,m_a,U_a,exp_a\rangle$，签发过程逐项消费 | 已验证 |
| R4 | 需要一个较为具体的 motivating example | 已将引言中的简短 Git 推送情形扩展为匿名的“多智能体博弈平台→DiOS 测试服务器”贯穿示例，覆盖事件、任务、能力、调用和上游副作用 | 已映射 `e,a,t,g,c`、`U(g)`、两个 PEP 及 RQ1/RQ2；设施名 Qame、demogo 不进入论文正文 | 已验证 |
| R5 | 需要定义治理目标 | 已定义 $\operatorname{GovOK}$ 为安全保持、正常可用性保持和可问责性的合取，避免以全拒绝获得表面安全 | 三个目标分量分别映射 I1--I4 与 RQ1--RQ3 | 已验证 |
| R6 | 句子之间衔接生硬 | 已按方法类与实证类论文的段落推进模式完成全文审计；补充问题--机制、对象语义--部署边界、证明--复杂度、基线--副作用实验等关系桥，并修复 RQ2 标题辖域 | 相邻段落能够说明下一步为何必要；标题覆盖其下机制或证据类型；无依靠机械连接词掩盖关系的修改 | 已验证 |

## 后续统一修订顺序

v1.1.25 已完成双遍 XeLaTeX 编译、18页逐页渲染、字体嵌入与 Unicode、交叉引用、匿名词扫描以及 LaTeX/Markdown 结构同步；本轮六项意见均已闭合。
