# E2AG 新增评审意见待处理（2026-08-16）

当前阶段只登记意见，不修改论文正文、LaTeX、Markdown、算法、配图或 PDF。

| 编号 | 评审意见 | 当前判断 | 后续验收条件 | 状态 |
|---|---|---|---|---|
| R1 | 形式化时需要定义什么是“层次” | 当前稿件定义了事件域、任务域、工具域及 TB1/TB2，但没有把授权层作为独立数学对象；且容易与治理、执行、证据三个架构平面混淆 | 定义授权层集合与对象所属层映射；明确“跨层”指事件声明、任务创建和工具调用之间的授权传播，架构平面不等同于授权层 | 待处理 |
| R2 | 形式化时需要定义什么是“能力” | 当前已有能力元组和 `ValidGrant`，但概念语义、签发条件和使用条件没有在字段结构之前闭合 | 给出任务作用域能力的概念定义、字段语义、签发谓词和使用谓词；主张不得超过实现实际提供的真实性与不可伪造保障 | 待处理 |
| R3 | 算法中的 notation 应在使用前定义 | 当前算法中的 `targets`、`C`、`P_a`、`m`、`U_a`、`exp`、时间参数及辅助过程没有全部在首次使用前定义，其中 `m`、`U_a`、`exp` 属于明显的先用后定义 | 在算法前增加符号/过程定义；使策略判定显式返回决策、服务、工具集合和有效期，逐项检查算法的输入、输出、局部量和辅助过程 | 待处理 |
| R4 | 需要一个较为具体的 motivating example | 已将引言中的简短 Git 推送情形扩展为匿名的“多智能体博弈平台→DiOS 测试服务器”贯穿示例，覆盖事件、任务、能力、调用和上游副作用 | 已映射 `e,a,t,g,c`、`U(g)`、两个 PEP 及 RQ1/RQ2；设施名 Qame、demogo 不进入论文正文 | 已处理，待 PDF 复核 |

## 后续统一修订顺序

1. 先补授权层与能力定义；
2. 再用 motivating example 引出跨层授权失配，并映射到形式化对象；
3. 随后闭合算法 notation、输入输出与辅助过程；
4. 最后同步引言、问题定义、算法、Markdown 和递增版本 PDF，并重新做引用、匿名性与版式检查。
