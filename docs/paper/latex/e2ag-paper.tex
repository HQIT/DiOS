\documentclass[UTF8,zihao=5,fontset=none]{ctexart}
\setCJKmainfont[Path=C:/Windows/Fonts/,BoldFont=NotoSerifSC-VF.ttf,ItalicFont=NotoSerifSC-VF.ttf]{NotoSerifSC-VF.ttf}
\setCJKsansfont[Path=C:/Windows/Fonts/,BoldFont={Noto Sans SC Bold (TrueType).otf}]{Noto Sans SC (TrueType).otf}
\setCJKmonofont[Path=C:/Windows/Fonts/]{Noto Sans SC (TrueType).otf}
\usepackage[a4paper,top=22mm,bottom=24mm,left=22mm,right=22mm]{geometry}
\usepackage{amsmath,amssymb,booktabs,longtable,array,graphicx,xcolor,url}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc,backgrounds}
\usepackage[hidelinks]{hyperref}
\hypersetup{
  pdftitle={E2AG: Event-to-Agent Governance},
  pdfauthor={},
  pdfsubject={Anonymous manuscript},
  pdfcreator={LaTeX}
}
\usepackage{enumitem}
\usepackage{fancyhdr}
\setlength{\headheight}{14pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small 软件学报投稿工作稿}
\fancyhead[R]{\small E2AG}
\fancyfoot[C]{\thepage}
\setlength{\parindent}{2em}
\setlength{\parskip}{0pt}
\setlist{nosep,leftmargin=2.2em}
\renewcommand{\arraystretch}{1.15}
\setlength{\emergencystretch}{2em}
\Urlmuskip=0mu plus 1mu
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\title{\bfseries\zihao{-2} E2AG：面向事件驱动智能体操作系统的能力契约接入、策略门控与因果审计}
\author{}
\date{}

\begin{document}
\maketitle

\begin{abstract}
事件驱动人工智能操作系统连接外部事件、智能体运行时与工具生态，但事件接入、任务创建和工具调用跨越多个信任边界。本文提出 E2AG（Event-to-Agent Governance）：将 Connector manifest 扩展为可执行的 source--event type 能力契约，在 A2A Task 创建前执行目标作用域三态策略，为任务签发短期工具授权并在 MCP 调用点再次强制，同时用统一 \texttt{trace\_id} 和哈希链接证据链记录契约、策略、调度、授权与工具结果。在作者冻结的 30 个正常和 30 个攻击事件上，无机制、仅契约、仅策略和契约加策略的攻击阻断率分别为 0\%、33.33\%、66.67\% 和 100\%，四组均通过 30/30 个正常事件。进一步执行 480 次真实 dispatcher、SQLite、A2A Task、ToolGrant 与 MCP PEP 路径：双执行点同时开启时，30/30 个正常调用到达模拟上游，伪造来源、任务后工具升级和生产敏感动作三类禁止副作用均为 0/30；只启用任一执行点会暴露其未覆盖阶段。5 类注入故障的 100 条持久化 trace 均定位到首个治理失败阶段并通过链校验。并发实验发现原始去重存在竞态；引入数据库唯一去重声明后，SQLite 下 8/32 并发各 100 轮均未产生重复日志或双重审批。结果证明的是威胁驱动确定性场景中的机制闭环，不代表任意提示词注入检测、跨数据库一致性或生产部署效果。

\noindent\textbf{关键词：} 人工智能操作系统；智能体安全；事件驱动系统；能力契约；策略门控；因果审计
\end{abstract}

\noindent\textbf{中图法分类号：} TP309

\begin{center}
{\bfseries\large E2AG: Capability-Contract Admission, Policy Gating, and Causal Auditing for Event-Driven Agent Operating Systems}
\end{center}

\begin{quote}\small
\textbf{Abstract:} Event-driven agent operating systems connect external events to agent runtimes and tools across multiple trust boundaries. We present E2AG (Event-to-Agent Governance), which turns connector manifests into executable source--event type contracts, performs target-scoped three-way policy decisions before A2A task creation, issues short-lived task-scoped tool grants, re-enforces them at the MCP call site, and records contract, policy, dispatch, grant, and tool outcomes in a trace-consistent hash-linked chain. In an author-frozen matrix of 30 benign and 30 attack events, attack prevention is 0\%, 33.33\%, 66.67\%, and 100\% for no mechanism, contract only, policy only, and contract plus policy; all configurations pass 30/30 benign events. Across 480 executions using the real DiOS dispatcher, SQLite models, A2A Task, ToolGrant, and MCP PEP paths, the two-PEP configuration preserves 30/30 authorized upstream effects while each of three forbidden-effect scenarios reaches the upstream 0/30 times. One hundred persisted traces across five injected fault stages are all localized correctly and remain hash-valid. After a replay race was found, a database-unique time-bounded deduplication claim yielded no duplicate log or double approval in 8- and 32-way SQLite tests over 100 rounds each. These results establish deterministic mechanism behavior, not general prompt-injection detection, cross-database correctness, or production effectiveness.

\textbf{Key words:} agent operating system; agent security; event-driven system; capability contract; policy enforcement; causal audit
\end{quote}

\input{e2ag-body.tex}
\end{document}
