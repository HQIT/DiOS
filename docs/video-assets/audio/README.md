# 旁白 AU-01

| 文件 | 说明 |
|------|------|
| `narration-full.txt` | 完整版口播文本 |
| `AU-01-narration.mp3` | edge-tts 合成（`zh-CN-YunxiNeural`，约 4 分钟） |

## 重新生成

```bash
./scripts/generate-video-narration-tts.py
```

首次运行会创建 `scripts/.venv-video` 并安装 `edge-tts`。

**AU-02 BGM**：按你的要求暂不制作。
